#!/bin/bash

{{- if eq .chezmoi.os "darwin" }}
(command -v /Applications/p4merge.app/Contents/MacOS/p4merge > /dev/null) && exec /Applications/p4merge.app/Contents/MacOS/p4merge "$@";
{{- end }}
(command -v p4merge > /dev/null) && exec p4merge "$@";
(command -v bc > /dev/null) && exec bc "$@";
