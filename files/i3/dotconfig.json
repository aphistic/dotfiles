{
    "vars": [
        { "name": "host", "exec": "hostname -s", "default": "default" }
    ],
    "merge": [
        {
            "output": "i3blocks.conf.symlink",
            "input": [
                "i3blocks.conf.base"
            ]
        }
    ]
}
