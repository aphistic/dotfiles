{
    "vars": [
        { "name": "host", "exec": "hostname -s" }
    ],
    "merge": [
        {
            "output": "config",
            "input": [
                "config.base",
                "config.host.{{host}}"
            ]
        }
    ]
}
