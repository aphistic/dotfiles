[user]
{{ if eq .Env.HM_TASK "nm" }}
	name = Erik Davidson
	email = erikdavidson@northwesternmutual.com
{{ else }}
    name = Erik Davidson
    email = erik@erikd.org
{{ end }}

[core]
	editor = vim
[merge]
    tool = p4merge
{{ if eq .Env.HM_TASK "nm" }}
[mergetool "p4merge"]
    cmd = /Applications/p4merge.app/Contents/Resources/launchp4merge $PWD/$BASE $PWD/$REMOTE $PWD/$LOCAL $PWD/$MERGED
    trustExitCode = false
{{ end }}
